// Intercepted (`(.)settings`) counterpart of `settings/investing/page.tsx` — identical
// content, reused verbatim; only the surrounding layout (`../layout.tsx`) differs.
export { default } from "../../../settings/investing/page";
