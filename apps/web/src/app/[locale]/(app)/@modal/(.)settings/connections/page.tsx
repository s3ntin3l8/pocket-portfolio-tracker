// Intercepted (`(.)settings`) counterpart of `settings/connections/page.tsx` — identical
// content, reused verbatim; only the surrounding layout (`../layout.tsx`) differs.
export { default } from "../../../settings/connections/page";
