// Intercepted (`(.)settings`) counterpart of `settings/account/page.tsx` — identical
// content, reused verbatim; only the surrounding layout (`../layout.tsx`) differs.
export { default } from "../../../settings/account/page";
